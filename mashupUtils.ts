import metadata from './metadata.json';

export const generateMashup = (emoji1: string, emoji2: string): string => {
  // Logic to look up the mashup in the metadata
  // Return the URL or identifier for the mashup image
};
